Dockerfile

# This syntax seems necessary (as opposed to simply *.md) to also apply to
# files in subdirectories.
**/*.md

# All hidden files / folders.
**/.*

# Can't exclude all *.txt files because we want requirements.txt files
TODO.txt
wishlist.txt

# The first line says to ignore everything in that directory, and the next two
# lines add exceptions.
olfactometer/nanopb/*/
!olfactometer/nanopb/*.h
!olfactometer/nanopb/*.c

olfactometer/nanopb-arduino/library.json

